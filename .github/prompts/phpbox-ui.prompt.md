---
mode: "agent"
---

You are handling a UI task for phpbox Desktop.

Follow these rules:
- Use the approved visual prototype and implementation spec as the ground truth.
- Keep layout, spacing, color tokens, motion, and text consistent with the design.
- Do not introduce new dependencies or “modernize” the UI away from the approved design.
- Verify the affected area with the smallest relevant UI and project validation command.
- When in doubt, prefer the project’s real design and CLI contract over a subjective improvement.

Before making changes, confirm the target behavior, the relevant implementation area, and the minimal validation path.
